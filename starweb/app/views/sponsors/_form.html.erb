<%= simple_form_for sponsor, defaults: { label_html: { class: 'font-weight-bold' } } do |form| %>
  <%= form.error_notification %>

  <%= form.input :name, label: 'Nombre' %>

  <%= form.input :image, label: 'Imagen' %>

  <%= form.submit 'Guardar', class: 'btn btn-primary' %>

<% end %>